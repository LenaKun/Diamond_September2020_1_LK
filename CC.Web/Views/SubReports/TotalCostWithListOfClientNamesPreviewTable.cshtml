@model IEnumerable<SubReportImportRowBase>

Clients:
@Html.Grid(Model).Columns(column=>
	{
		column.For(f=>f.Index);
		column.For(f=>f.ClientId).Named("CCID");
		column.For(f=>f.ClientName);
		column.For(f=>Html.UnsortedList(f.Errors));
	})
