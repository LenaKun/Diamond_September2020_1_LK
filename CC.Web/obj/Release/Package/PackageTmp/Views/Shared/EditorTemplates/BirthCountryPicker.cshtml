@model int?
@{
	if (ViewBag.BirthCountries == null)
	{
		using (var db = new ccEntities())
		{
			ViewBag.BirthCountries = new SelectList(db.BirthCountries.OrderBy(f => f.Name).ToList(), "Id", "Name", Model);
		}
	}
}
@Html.DropDownList("", (SelectList)ViewBag.BirthCountries, "")
